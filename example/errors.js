var CrowdProcess = require('..');

function Run(d) {
  e
  return d;
}

var data = [1, 2, 3];

var crp = new CrowdProcess('mail@example.com', 'password');

crp.map(Run, data, function(err, result) {
  console.log(err);
});
